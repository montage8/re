# 보안 체크리스트

시스템을 배포하기 전에 다음 보안 사항을 확인하세요.

## 필수 보안 사항

### 1. 데이터베이스 보안
- [ ] `config.php` 파일이 생성되었고 올바른 데이터베이스 정보가 입력됨
- [ ] 데이터베이스 사용자 비밀번호가 강력함 (최소 16자, 복잡한 조합)
- [ ] 데이터베이스 사용자 권한이 필요한 것만 부여됨 (SELECT, INSERT, UPDATE만)
- [ ] MySQL 포트(3306)가 외부에서 접근 불가능하도록 방화벽 설정됨
- [ ] `config.php` 파일 권한이 600으로 설정됨 (`chmod 600 config.php`)

### 2. 웹 서버 보안
- [ ] HTTPS가 활성화되어 있음 (SSL/TLS 인증서 설치)
- [ ] `.htaccess` 파일이 활성화되어 민감한 파일 접근이 차단됨
- [ ] Apache AllowOverride가 활성화되어 있음
- [ ] 디렉토리 리스팅이 비활성화되어 있음
- [ ] PHP 에러 표시가 프로덕션 환경에서 비활성화됨 (`display_errors = Off`)

### 3. 인증 보안
- [ ] 기본 관리자 비밀번호가 변경됨
- [ ] 새 비밀번호가 강력함 (최소 12자, 대소문자, 숫자, 특수문자 포함)
- [ ] 세션 쿠키 설정이 안전함 (HttpOnly, Secure, SameSite)
- [ ] 세션 타임아웃이 설정되어 있음 (기본 30분)

### 4. 파일 보안
- [ ] `config.php`가 Git에 커밋되지 않음 (.gitignore에 포함됨)
- [ ] `database_setup.sql` 파일이 웹에서 접근 불가능함
- [ ] 백업 파일(*.bak, *.backup)이 웹에서 접근 불가능함
- [ ] 로그 파일이 웹에서 접근 불가능함

## 권장 보안 사항

### 5. 모니터링 및 로깅
- [ ] PHP 에러 로그가 활성화되어 있음
- [ ] 로그 파일이 정기적으로 모니터링됨
- [ ] 의심스러운 로그인 시도가 알림으로 전송됨
- [ ] `activity_logs` 테이블이 정기적으로 검토됨
- [ ] 실패한 로그인 시도가 5회 초과 시 자동 차단됨

### 6. 백업
- [ ] 데이터베이스 자동 백업이 설정됨 (일일 권장)
- [ ] 백업 파일이 안전한 위치에 저장됨
- [ ] 백업 복원 절차가 테스트됨
- [ ] 백업 파일이 암호화되어 있음

### 7. 네트워크 보안
- [ ] 방화벽이 설정되어 필요한 포트만 열려 있음 (80/443만)
- [ ] SSH 포트가 변경되었거나 키 기반 인증만 허용됨
- [ ] fail2ban 또는 유사한 침입 방지 시스템이 설치됨
- [ ] DDoS 방어 시스템이 활성화됨 (Cloudflare 등)

### 8. 소프트웨어 업데이트
- [ ] PHP가 최신 버전임 (최소 7.4 이상, 8.0 이상 권장)
- [ ] MySQL/MariaDB가 최신 버전임
- [ ] Apache/Nginx가 최신 버전임
- [ ] 운영체제 보안 패치가 최신임

### 9. 추가 보안 기능
- [ ] IP 화이트리스트 고려됨 (필요시)
- [ ] 2단계 인증(2FA) 구현 고려됨
- [ ] 비밀번호 정기 변경 정책 수립됨 (3개월마다 권장)
- [ ] 침입 탐지 시스템(IDS) 설치 고려됨

### 10. 문서화
- [ ] 관리자 연락처가 문서화됨
- [ ] 비상 대응 절차가 문서화됨
- [ ] 데이터베이스 복원 절차가 문서화됨
- [ ] 보안 사고 대응 계획이 수립됨

## 배포 전 최종 확인

### 데이터베이스
```bash
# 데이터베이스 연결 테스트
php -r "require 'config.php'; getDbConnection(); echo 'OK';"
```

### 파일 권한
```bash
# config.php 권한 확인
ls -l config.php
# 출력: -rw------- (600)

# 민감한 파일 웹 접근 테스트
curl http://your-domain.com/config.php
# 출력: Forbidden 또는 403 에러
```

### HTTPS
```bash
# SSL 인증서 확인
curl -I https://your-domain.com
# 출력: HTTP/2 200 (또는 HTTP/1.1 200)
```

### 로그인 제한
1. 잘못된 비밀번호로 5번 로그인 시도
2. 6번째 시도 시 차단되는지 확인
3. 15분 후 다시 로그인 가능한지 확인

### CSRF 보호
1. 브라우저 개발자 도구로 폼 확인
2. csrf_token 필드가 있는지 확인
3. 토큰 없이 POST 요청 시 차단되는지 확인

## 정기 점검 (월 1회)

- [ ] 로그 파일 검토
- [ ] 백업 확인
- [ ] 보안 업데이트 적용
- [ ] 비밀번호 강도 재확인
- [ ] 의심스러운 활동 확인

## 보안 사고 발생 시

1. 즉시 시스템 격리 (네트워크 차단)
2. 최근 로그 확인 및 저장
3. 침해 범위 파악
4. 비밀번호 즉시 변경
5. 백업으로부터 복원
6. 취약점 패치
7. 사고 보고서 작성

## 문의

보안 관련 문의: [시스템 관리자 이메일]
긴급 연락처: [긴급 연락처]
